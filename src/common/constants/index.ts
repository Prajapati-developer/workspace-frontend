export const SYSTEM_CONSTANTS = {
  APP_NAME: import.meta.env.VITE_REACT_APP_NAME,
};

export enum ROLE {
  ADMIN = "admin",
  USER = "user",
  EMPLOYEE = "employee",
  SUPER_ADMIN = "super_admin",
}
